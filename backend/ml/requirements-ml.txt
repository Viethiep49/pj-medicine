# ML Data Pipeline — Dependencies
# Dùng cho data_pipeline.py và drugpred_kaggle_train.ipynb
#
# QUAN TRỌNG: File này chạy RIÊNG BIỆT trên máy local hoặc Kaggle.
# KHÔNG cài vào backend server — backend chỉ cần inference (requirements.txt).
#
# Cách dùng:
#   pip install -r backend/ml/requirements-ml.txt
#   python backend/ml/data_pipeline.py all

# Data processing
pandas>=2.2.0
scikit-learn>=1.4.0
matplotlib>=3.9.0

# Dataset download
kagglehub>=0.3.0
datasets>=2.19.0          # HuggingFace datasets (tải ViMedAQA, HoangHa)

# Training (Kaggle đã có sẵn torch + transformers, uncomment khi train local)
# torch>=2.3.0
# transformers>=4.40.0
# peft>=0.11.0
# sentencepiece>=0.2.0

# Vietnamese NLP (dùng trong pipeline xử lý text)
underthesea>=6.8.0
